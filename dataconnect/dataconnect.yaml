specVersion: "v1"
serviceId: "chessgame"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "chessgame-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
